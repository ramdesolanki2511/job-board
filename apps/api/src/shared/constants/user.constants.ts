export enum UserRole {
  ADMIN = "admin",
  USER = "user",
}

export enum AuthProvider {
  LOCAL = "local",
  GOOGLE = "google",
}

export enum SubscriptionStatus {
  NONE = "none",
  ACTIVE = "active",
  EXPIRED = "expired",
  CANCELLED = "cancelled",
}